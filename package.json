{
  "name": "ondex",
  "private": true,
<<<<<<< HEAD
  "type": "module",
  "scripts": {
    "build": "pnpm run build:contracts && pnpm run build:ts",
    "build:contracts": "cargo build --target wasm32v1-none --release --manifest-path ./contracts/Cargo.toml",
    "build:ts": "pnpm -r build",
    "deploy": "tsx scripts/deploy.ts",
    "fund": "tsx scripts/fund-testnet-account.ts"
  },
  "devDependencies": {
    "@stellar/stellar-sdk": "^13.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
=======
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "contracts:build": "cd contracts && cargo build --target wasm32-unknown-unknown --release",
    "contracts:deploy": "node scripts/deploy.mjs",
    "bindings:generate": "node scripts/generate-bindings.mjs"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  },
  "packageManager": "pnpm@9.0.0"
>>>>>>> b2133f6 (Initial frontend setup)
}
